<%=content_tag :ul, id: "comments-list" do %>
  <% comments.each do |comment| %>
    <%=content_tag(:li,
      content_tag(:p,comment.user.email) + content_tag(:p,comment.content),class:"comment-panel") %>
  <% end %>
<% end %>
